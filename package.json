{
  "name": "scrcpy-sidebar",
  "displayName": "Scrcpy Sidebar",
  "description": "Display and control remote Android devices from a VS Code sidebar in code-server.",
  "version": "0.1.0",
  "publisher": "local",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/dream10201/scrcpy_sidebar"
  },
  "homepage": "https://github.com/dream10201/scrcpy_sidebar",
  "bugs": {
    "url": "https://github.com/dream10201/scrcpy_sidebar/issues"
  },
  "type": "commonjs",
  "files": [
    "dist/**",
    "media/**",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:scrcpySidebar.view",
    "onCommand:scrcpySidebar.selectDevice",
    "onCommand:scrcpySidebar.disconnect",
    "onCommand:scrcpySidebar.reconnect"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "scrcpySidebar",
          "title": "Android Screen",
          "icon": "media/android.svg"
        }
      ]
    },
    "views": {
      "scrcpySidebar": [
        {
          "id": "scrcpySidebar.view",
          "name": "Devices",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "scrcpySidebar.selectDevice",
        "title": "Scrcpy Sidebar: Select Device"
      },
      {
        "command": "scrcpySidebar.disconnect",
        "title": "Scrcpy Sidebar: Disconnect Device"
      },
      {
        "command": "scrcpySidebar.reconnect",
        "title": "Scrcpy Sidebar: Reconnect Device"
      }
    ],
    "configuration": {
      "title": "Scrcpy Sidebar",
      "properties": {
        "scrcpySidebar.adbHost": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "ADB server host."
        },
        "scrcpySidebar.adbPort": {
          "type": "number",
          "default": 5037,
          "description": "ADB server port."
        },
        "scrcpySidebar.scrcpyServerVersion": {
          "type": "string",
          "default": "4.0",
          "description": "scrcpy-server version downloaded during dependency installation."
        },
        "scrcpySidebar.maxFps": {
          "type": "number",
          "default": 15,
          "minimum": 0,
          "description": "Default maximum FPS. 0 lets scrcpy choose."
        },
        "scrcpySidebar.maxSize": {
          "type": "number",
          "default": 960,
          "minimum": 0,
          "description": "Default maximum video size. 0 keeps original size."
        },
        "scrcpySidebar.videoBitRate": {
          "type": "number",
          "default": 2000000,
          "minimum": 1000000,
          "description": "Default scrcpy video bitrate."
        },
        "scrcpySidebar.videoCodec": {
          "type": "string",
          "enum": [
            "h264",
            "h265",
            "av1"
          ],
          "default": "h264",
          "description": "Preferred video codec."
        },
        "scrcpySidebar.videoBufferMs": {
          "type": "number",
          "default": 50,
          "minimum": 0,
          "description": "Client-side video buffer in milliseconds before decoding. 0 minimizes latency; higher values smooth unstable transport."
        },
        "scrcpySidebar.autoReconnectDelayMs": {
          "type": "number",
          "default": 3000,
          "minimum": 1000,
          "description": "Delay before trying to reconnect after disconnect."
        },
        "scrcpySidebar.rootMode": {
          "type": "string",
          "enum": [
            "auto",
            "always",
            "never"
          ],
          "default": "auto",
          "description": "How scrcpy server should use su/root. auto starts without root and falls back to root only when input injection is denied."
        },
        "scrcpySidebar.screenOffOnStart": {
          "type": "boolean",
          "default": true,
          "description": "Blank the physical device screen after the video stream is ready."
        },
        "scrcpySidebar.keepScreenAwake": {
          "type": "boolean",
          "default": true,
          "description": "Keep the physical device awake while connected. Uses scrcpy stay_awake and a temporary 24h screen_off_timeout."
        },
        "scrcpySidebar.keepActive": {
          "type": "boolean",
          "default": true,
          "description": "Keep the display active by asking scrcpy 4.x to periodically signal user activity."
        },
        "scrcpySidebar.flexDisplay": {
          "type": "boolean",
          "default": false,
          "description": "Use scrcpy 4.x flex display mode with a new virtual display resized to the sidebar. This is mainly useful when launching a specific app on that display."
        },
        "scrcpySidebar.powerOnOnStart": {
          "type": "boolean",
          "default": false,
          "description": "Request the physical device screen to wake before streaming starts."
        },
        "scrcpySidebar.powerOffOnClose": {
          "type": "boolean",
          "default": true,
          "description": "Turn off the physical device screen when the session closes."
        },
        "scrcpySidebar.audioEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable physical device audio streaming."
        },
        "scrcpySidebar.audioCodec": {
          "type": "string",
          "enum": [
            "opus",
            "aac"
          ],
          "default": "aac",
          "description": "Preferred audio codec for experimental browser playback."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build",
    "package": "npm run build && npx @vscode/vsce package",
    "prepare": "npm run build",
    "postinstall": "node node_modules/@yume-chan/fetch-scrcpy-server/bin/fetch-server.js 4.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.25.11",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@yume-chan/adb": "^2.6.0",
    "@yume-chan/adb-scrcpy": "^2.3.2",
    "@yume-chan/adb-server-node-tcp": "^2.5.2",
    "@yume-chan/fetch-scrcpy-server": "^1.0.0",
    "@yume-chan/scrcpy": "^2.3.0",
    "@yume-chan/scrcpy-decoder-webcodecs": "^2.5.3"
  }
}
